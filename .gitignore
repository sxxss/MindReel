node_modules
dist
build
.traces
.DS_Store
*.log

# 密钥与本地配置（务必不要提交）
**/.env
**/.env.local
**/providers.json

# 包管理 / 本地缓存
.pnpm-store
.tts-venv

# 运行时数据与生成产物
data

# 本地 Docker 开发覆盖（自带 TTS 等，不进仓库）
docker-compose.override.yml

# Python 后端
server/.venv
__pycache__/
*.pyc
